interface:
  display_name: "interseo"
  short_description: "SEO CLI con DNS y MCP"
  default_prompt: "Use $interseo to audit a domain from the CLI, check DNS and SEO, and generate a Google-ready kit."